name: egtaxi
description: EGTaxi - Flutter frontend connected to Python FastAPI backend
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  http: ^0.13.6
  web_socket_channel: ^2.3.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/car_background.jpeg